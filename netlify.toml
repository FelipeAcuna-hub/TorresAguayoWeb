[build]
  command = "mkdir -p web_dist && cp -r components img pdf *.html *.js web_dist 2>/dev/null || true"
  publish = "web_dist"

[[plugins]]
  package = "@netlify/plugin-nextjs"
  [plugins.inputs]
    disable = true